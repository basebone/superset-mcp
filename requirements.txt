# Generated. Do not edit by hand, and do not add a dependency here -- add it to
# pyproject.toml's `dependencies` and run `make lock` in this directory.
#
# This is a lock: the exact versions pyproject.toml's floors resolved to for CPython 3.10
# on linux, which is what the virtualenv deployed on superset1 runs.
#
# What it replaced was a `pip freeze` -- its own header said "Regenerate with: pip freeze
# > requirements.txt (from a known-good venv)". That is a snapshot of one machine, and it
# is how this virtualenv came to carry 56 advisories on 2026-09-08 while every one of them
# was one or two releases from a fix.
#
# uv.lock is also in this repository, from whoever develops with uv. It is not what the
# deployment installs and nothing here regenerates it.
#
# Locked on 2026-09-08.
# --- pins below, generated by `make lock` ---
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
cryptography==50.0.1
exceptiongroup==1.3.1
fastapi==0.141.1
google-auth==2.57.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
httpx-sse==0.4.3
idna==3.19
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
markdown-it-py==4.2.0
mcp==1.30.0
mdurl==0.1.2
packaging==26.3
pyasn1==0.6.4
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
pydantic-settings==2.15.0
Pygments==2.21.0
PyJWT==2.13.0
python-dotenv==1.2.3
python-multipart==0.0.32
referencing==0.37.0
requests==2.34.2
rich==15.0.0
rpds-py==0.30.0
setuptools==84.0.0
shellingham==1.5.4
sse-starlette==3.4.11
starlette==1.6.0
typer==0.27.2
typing_extensions==4.16.0
typing-inspection==0.4.4
urllib3==2.7.0
uvicorn==0.52.4
